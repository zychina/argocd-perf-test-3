apiVersion: v1
kind: Service
metadata:
  name: perf-test3-app205-svc
  namespace: clawtest
  labels:
    app: perf-test3-app205
spec:
  selector:
    app: perf-test3-app205
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
