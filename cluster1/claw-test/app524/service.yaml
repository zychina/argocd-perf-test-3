apiVersion: v1
kind: Service
metadata:
  name: perf-test3-app524-svc
  namespace: clawtest
  labels:
    app: perf-test3-app524
spec:
  selector:
    app: perf-test3-app524
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
